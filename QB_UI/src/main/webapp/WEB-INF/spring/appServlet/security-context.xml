<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
  http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- 로그인 -->

	<security:http auto-config="true">
		<security:csrf disabled="true" />
		<security:form-login
			username-parameter="username"
			password-parameter="password"
			login-page="/login.jsp"
			authentication-failure-url="/login.jsp?error"
			
			/>
			
		<security:logout logout-success-url="/login.jsp" />
		<security:session-management 
			invalid-session-url="/login.jsp">
			<security:concurrency-control max-sessions="1"
				expired-url="/login.jsp" />
		</security:session-management>

	</security:http>
	<!-- 로그인 완료후 처리할 handler -->
	

	<security:authentication-manager>
		<security:authentication-provider>
			<security:jdbc-user-service
				data-source-ref="driverManagerDataSource"
				users-by-username-query="SELECT member_id, member_pwd AS PASSWORD, 1 enabled FROM member where member_id=?"
				authorities-by-username-query="select m.member_id , r.role_code  
           									  from member m join role r 
          									 on m.member_id = r.role_desc 
          									 where m.member_id=?" />
		</security:authentication-provider>
	</security:authentication-manager>

</beans>
