diff a/QB_UI/src/main/java/onet/com/mappers/commonMapper.xml b/QB_UI/src/main/java/onet/com/mappers/commonMapper.xml	(rejected hunks)
@@ -194,10 +194,12 @@
 
 <!-- 학생 차트 정보 -->
 <select id="studentChartInfo" parameterType="String" resultType="onet.com.vo.Score_chartDto">
-	select * from score_chart 
-	where member_id=#{member_id} and class_name = 
-	(select class_name from member where member_id=#{member_id})
-	order by member_id, exam_info_num asc
+	select * 
+	from score_chart sc JOIN member m
+	ON sc.member_id = m.member_id
+	where m.member_name=#{member_name} and sc.class_name = 
+	(select class_name from member where member_name=#{member_name})
+	order by m.member_name, sc.exam_info_num asc
 </select>
 <!-- 클래스 차트 정보 -->
 <select id="classChartInfo" parameterType="String" resultType="onet.com.vo.Class_chartDto">
